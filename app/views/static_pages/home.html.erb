<body class="homes">
  <% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/stats' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="span8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
    <div class="center hero-unit">
  <h1>Добро пожаловать в BSUIR blogs</h1>

  <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>

  <h2>
    Будь на связи с друзьями. Получай свежие новости о том, что происходит в университете.
    И следи за событиями в реальном времени, с разных сторон.
  </h2>
  
</div>
<%end%>
</body>